# DECISIONS LOG

Judgment calls made autonomously, per the brief's operating rules. Newest last.

1. **Missing prototype — rebuild from spec.** `reference/plastic-platoon-prototype.html` does not exist in the repo and was not found anywhere on this machine. The brief's §3.1 (core loop description) and §3.2 (exact tuning numbers) fully specify it, so mechanics are rebuilt directly from those sections instead of blocking on the owner. If the file turns up later, diff behavior against it.

2. **Stack: PixiJS v8 + Vite + TypeScript** (the brief's preferred option). WebGL sprite batching + ParticleContainer trivially clears the 200-units-plus-particles @60fps bar, and the tilt-shift pass is cheap as pre-blurred overlay sprites. Vanilla canvas was viable but puts atlas batching, pooling, and the blur pass entirely by hand for no gain.

3. **UI is a DOM overlay, not canvas.** The §4.5 toy-packaging kit (chunky extruded buttons, sticker labels, cards, price tags) is native CSS territory: cheaper to build, crisper text, free accessibility, easy bounce/snap animations. Canvas renders the world; DOM renders HUD/shop/popups. Scrap pips fly in-canvas and finish at the DOM counter's screen position.

4. **Screenshot loop + perf probe via Playwright** (npm dev dep, chromium). Deterministic dev hooks: query params + seeded RNG + sim fast-forward API (`window.__pp`) so "first kill / 60s mid-battle / upgrade purchase" shots are reproducible. Perf probe fast-forwards the sim to a high-wave worst case, then measures ~30s of real render frames and asserts p95 < 16.7 ms — 3 sim-minutes of load are covered by fast-forward; measuring 3 real-time minutes per run would only add waiting, not information. Headless GL is SwiftShader (software); probe logs the renderer string and results carry a real-device caveat, as the brief anticipates.

5. **Minimal service worker included.** Network-first for `index.html`, cache-first for content-hashed assets. Gives instant repeat loads + offline launch from home screen without the stale-build-badge risk of cache-first HTML.

6. **M2 gate adaptation.** The brief asks for a screen-recording review of the juice pass. No screen-recording tooling is available headlessly on this setup; substituted: rapid screenshot bursts around each juice event + a fresh-context subagent auditing the implementation event-by-event against the §4.4 table.

7. **Repo name `plastic-platoon`**, public, under `nicholaspipin`. Public because GitHub Pages on private repos requires a paid plan feature; nothing sensitive ships.

8. **No fail state.** Tans halt at a stop line (`LAYOUT.tanStopX`) near the Molder rather than destroying it — a stalled push reads as "buy an upgrade", not "game over". Idle games punish loss aversion poorly; the brief is silent on lose conditions, so the friendliest interpretation wins. Greens likewise never chase targets past 86% of screen width, so the firing line stays on camera.

9. **M1 findings.** (a) Pixi gotcha: `Texture.from(canvas)` fixes the frame at creation — setting `source.resolution` afterwards does NOT shrink the logical size; full-screen baked textures (ground, tilt-shift strips) get explicit sprite width/height instead. (b) Units cluster into unreadable mush without spacing — added an O(n²) same-faction separation pass (~19px min distance, vertical-biased so rank depth survives); probe confirms it's free at 110 units. (c) Hold lines are per-unit staggered (±65–70px via each unit's phase) so formations read as ragged ranks; the green advance clamp is 0.68·W so firefights happen mid-frame instead of at the screen edge. (d) Baked props live inside the ground composite so the pre-blurred tilt-shift strips blur them for free; standing props are placed above the battle band to avoid occlusion errors.

10. **⚠ Parallel agent session detected — resolved without data loss.** While this session was mid-M1, a *different* AI agent session (its decision log references OpenAI site-hosting tooling; no Claude co-author trailers) pushed three commits to `origin/main`: a batched "M1-M3 vertical slice" that deleted this session's entire modular architecture in favor of one 1,789-line file, a workflow fix, and an Expo/React Native shell added to production dependencies. The Expo shell contradicts the brief (§2: mobile *web*, static Pages, no native shell; batching contradicts §6 "Do not batch"). Resolution: that entire line is preserved untouched on branch **`alt/parallel-vertical-slice`**; `main` continues this session's brief-compliant, gate-verified line via a `merge -s ours` (both histories are ancestors of main — nothing was force-pushed or deleted). To adopt the other line instead: `git checkout alt/parallel-vertical-slice`. **Owner should avoid running two agent sessions against the same repo simultaneously.**

11. **M2 gate ran as a code audit, verdict: all 8 juice-table rows covered.** A fresh-context subagent traced every sim event through main → renderer/hud/sfx with file:line evidence. Its gaps were all fixed: shake now takes the strongest overlapping request instead of summing (max, not +), boss deaths flash for reduced-motion users in place of hit-stop/shake, stamp shake raised to perceptible (~1.3px), the pip-pool-exhaustion fallback now emits a collect event (no silent increments), the band anticipation ring matches the 80ms snap delay, `prefers-reduced-motion` is live-tracked, and the previously dead `hit` event now drives a victim flinch squash.

12. **Offline earnings are also granted on visibility return, not just page load.** iOS home-screen PWAs resume from memory without reloading, so a load-time-only check would never pay out. On `visibilitychange: hidden` the time is stamped; on return after >60s away the same receipt card shows. Discovered while chasing a test failure that turned out to be `vite preview` SPA-fallbacking a bogus "no-app" URL to the real app, whose pagehide save overwrote the test's tampered timestamp.

13. **Parallel session, round 2 — integrated this time.** The other agent re-applied its Expo shell cleanly *on top of* this session's main (preserving the game, scripts, and workflow), so it was integrated rather than superseded: M2/M3 were rebased onto it. The Expo wrapper (webview around the deployed Pages URL) stays out of the web bundle; costs are heavier `npm ci` in deploys and a TS 5.7→6.0 bump, both verified green (build + shots + save-flow + perf all pass). The web game remains the source of truth per the brief.

14. **Final audit round (fresh-context perf audit + code review) — all HIGH/MED findings fixed.** Highlights: (a) Pixi v8 `destroy({texture:true})` does NOT free the TextureSource or backing canvas — zone rebakes leaked ~18MB each until `textureSource: true` was added; (b) tilt-shift blur now runs on only the two strip regions at half resolution (~8× cheaper rebakes, which also run on viewport resize now — iOS toolbar collapse used to leave a stale mis-sized diorama); (c) offline earnings are granted immediately on return with the receipt card purely presentational — the save's `lastSeen` advances within 5s, so a claim-gated grant was silently forfeited by any reload before the tap; (d) iOS's non-standard `'interrupted'` AudioContext state is now recovered from (was: audio permanently dead after a phone call); (e) HUD writes DOM only on value changes, collect pulses/blips are coalesced/rate-limited (a band snap could force 40+ reflows and clipping audio in one frame); (f) per-frame template-string texture keys replaced with pre-resolved arrays (~11k string allocs/sec at cap); (g) corrupt negative save values are clamped (a negative `greenReserve` would have made rifles heal enemies — permanent softlock); (h) module-preload links injected for Pixi's dynamic chunks (removes 2 serial RTTs from cold load); (i) gloss sweep animates transform (compositor) instead of background-position (continuous repaint). Known-and-accepted LOWs: DPR pinned at init; additive celebration sprite briefly splits one batch.

15. **Perf probe thresholds account for vsync.** rAF frame *deltas* on a healthy 60Hz run are exactly ~16.7ms, so asserting `p95 < 16.7` fails a perfect run. Bar is p95 < 17.5ms (one vsync + 5% jitter) plus p99 < 25ms (hitch guard), measured over a pure-render window; a second window with synthetic taps is reported but not gated, since Playwright's CDP input injection itself causes 50–100ms spikes (measured: game pure-render p99 = 16.8ms while taps-window max = 100ms). Probe machine exposes a real GPU (Intel Arc via ANGLE), so the SwiftShader caveat mostly doesn't apply here; on-device verification still recommended.

16. **Directive 2 army-feel pass: visible formation plates over pure simulation fidelity.** The sim now uses rank/column slots and front-rank fire discipline, but still screenshots needed a stronger physical cue for the baseplate rule. Added translucent green platoon baseplates under each formation block and moved the chalk command line under the toys so it reads like a floor mark, not a UI boundary. The Molder was moved in from the crop edge, scaled up, hero-lit, and rendered above the supply column after fresh review found it too buried.
